name: Keep Zero Dark Claims Awake

on:
  schedule:
    - cron: "0 */3 * * *"   # every 3 hours (UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Zero Dark Claims app
        run: curl -s -o /dev/null https://zero-dark-claims-tool.streamlit.app/
